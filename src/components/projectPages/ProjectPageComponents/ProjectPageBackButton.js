import React from 'react';
import {Box, Typography} from "@mui/material";

function ProjectPageBackButton(props) {
    return (
        <Box bgcolor={"#303030"}>
            <Typography variant={"h4"} p={2}>
                Back
            </Typography>
        </Box>
    );
}

export default ProjectPageBackButton;